<!--start.wxml-->
<view class="container" wx:for="{{sales}}" wx:key="id">
    <view class="tab-image">
        <image class="bgImages" mode="aspectFill" src="../../images/weCome.jpeg"/>
        <view class="gohome" bindtap="home">跳过广告 {{second}}</view>
    </view>
    <!--  <view class="logo">-->
    <!--    <view class="title">{{item.title}}</view>-->
    <!--    <view class="subtitle">{{item.remark}}</view>-->
    <!--  </view>-->
</view>
